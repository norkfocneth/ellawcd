# ──────────────────────────────────────────────
# Project Ella v1.0 — Dependencies
# Phase 1-3: Foundation + Memory + Voice + Session
# ──────────────────────────────────────────────

# ── Core ───────────────────────────────────────
requests>=2.31.0          # Ollama HTTP API client
loguru>=0.7.0             # Structured logging
rich>=13.0.0              # Beautiful terminal UI
python-dotenv>=1.0.0      # Environment variables

# ── Voice Output (TTS) ────────────────────────
edge-tts>=6.1.0           # Neural TTS (Microsoft Edge)

# ── Voice Input (STT) ─────────────────────────
faster-whisper>=1.0.0     # Whisper STT (GPU/CPU)
sounddevice>=0.4.6        # Microphone capture
numpy>=1.24.0             # Audio processing

# ── Future Phases (install when needed) ────────
# Phase 4: Tool Calling
# psutil>=5.9.0           # Process management

# Phase 5: Automation
# pyautogui>=0.9.54       # Mouse/keyboard automation
# pywinauto>=0.6.8        # Windows automation

# Phase 8: Vision
# mss>=9.0.0              # Screenshot capture
# easyocr>=1.7.0          # OCR text extraction
# Pillow>=10.0.0          # Image processing
# opencv-python>=4.8.0    # UI detection

# Phase 10: Browser
# playwright>=1.40.0      # Browser automation

# Phase 11: Files/PDF
# PyMuPDF>=1.23.0         # PDF reading
