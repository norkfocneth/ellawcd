# ──────────────────────────────────────────────
# Project Ella v1.0 — Dependencies
# Phase 1-11: Full OS Upgrade
# ──────────────────────────────────────────────

# ── Core ───────────────────────────────────────
requests>=2.31.0          # Ollama HTTP API client
loguru>=0.7.0             # Structured logging
rich>=13.0.0              # Beautiful terminal UI
python-dotenv>=1.0.0      # Environment variables
scipy>=1.10.0             # Scientific computing for audio DSP

# ── ONNX & GPU Execution ──────────────────────
onnxruntime-gpu>=1.16.0   # ONNX GPU acceleration (CUDA)

# ── Voice Output (TTS) ────────────────────────
kokoro-onnx>=0.1.0        # Kokoro Offline TTS
misaki[en]>=0.1.0         # Phonemizer for Kokoro (pure Python)

# ── Voice Input (STT) ─────────────────────────
faster-whisper>=1.0.0     # Whisper STT (GPU/CPU)
sounddevice>=0.4.6        # Microphone capture
numpy>=1.24.0             # Audio processing
silero-vad>=5.1.0         # Silero Voice Activity Detection (VAD)
noisereduce>=3.0.0        # Audio noise reduction

# ── Vision & UI Detection ─────────────────────
mss>=9.0.0                # High-speed screenshot capture
easyocr>=1.7.0            # Offline OCR
Pillow>=10.0.0            # Image handling
opencv-python>=4.8.0      # Template matching and UI contour detection

# ── Browser Automation ────────────────────────
playwright>=1.40.0        # Playwright browser controller

# ── Desktop Automation ────────────────────────
pywinauto>=0.6.8          # Native Windows Accessibility GUI controller
pyautogui>=0.9.54         # Mouse and keyboard input fallback
psutil>=5.9.0             # Windows Process manager

# ── Files/PDF ─────────────────────────────────
PyMuPDF>=1.23.0           # PDF reading

